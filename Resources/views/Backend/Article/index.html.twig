{% extends 'SyliusWebBundle:Backend:layout.html.twig' %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusWebBundle:Backend/Macros:misc.html.twig' import pagination %}
{% from 'WebburzaSyliusArticleBundle:Backend/Macros:macros.html.twig' import list %}

{% block topbar %}
    <ol class="breadcrumb">
        <li>{{ 'sylius.breadcrumb.content'|trans }}</li>
        <li>{{ 'webburza.sylius.article.breadcrumb.index'|trans }}</a></li>
    </ol>
{% endblock %}

{% block content %}
    <div class="page-header">
        <div class="actions-menu">
            {{ buttons.create(path('webburza_article_create'), 'webburza.sylius.article.create'|trans) }}
        </div>
        <h1>
            <i class="glyphicon glyphicon-file"></i> {{ 'webburza.sylius.article.index_header'|trans|raw }}
        </h1>
    </div>

    {{ pagination(articles) }}
    {{ list(articles) }}
    {{ pagination(articles) }}

{% endblock %}
